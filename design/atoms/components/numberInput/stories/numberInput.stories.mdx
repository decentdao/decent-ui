import NumberInput from "../src"
import { Canvas, Meta, Story } from "@storybook/addon-docs"

import { argTypes } from "./argTypes"

<Meta title="atoms/Input" component={NumberInput} />

export const Template = (args) => <NumberInput {...args} />

<Canvas>
  <Story
    name="NumberInput"
    argTypes={argTypes}
    args={{
      placeholder: "12345...",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
