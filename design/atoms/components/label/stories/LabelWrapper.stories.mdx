import { Canvas, Meta, Story } from "@storybook/addon-docs"
import { Input } from "@chakra-ui/react"

import LabelWrapper from "../src"

<Meta title="atoms/LabelWrapper" component={LabelWrapper} />

export const Template = (args) => {
  return (
    <LabelWrapper {...args}>
      <Input isDisabled={args.isDisabled} isInvalid={args.isErrored} placeholder="Placeholder" />
    </LabelWrapper>
  )
}

<Canvas>
  <Story
    name="LabelWrapper"
    argTypes={{
      label: {
        description: "Label that appears below component",
      },
      subLabel: {
        type: "string",
        description: "Label that appears below component",
      },
      tooltipContent: {
        type: "string",
        description:
          "`string` or `JSX.Element`. Content shown inside of tooltip when hovering icon. If left `undefined` the icon will not show",
      },
      errorMessage: {
        type: "string",
        description: "Error message that appears below component",
      },
      isDisabled: {
        type: "boolean",
        description: "If true will display an disabled state",
      },
      isInvalid: {
        type: "boolean",
        description: "If true will display an error state",
      },
      htmlFor: {
        type: "string",
        description: "Refers to the id of the element this label is associated with",
      },
      children: {
        description:
          "Accepts a React component as a child, wrapping the component with a label and sub label",
      },
    }}
    args={{
      label: "Label",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
