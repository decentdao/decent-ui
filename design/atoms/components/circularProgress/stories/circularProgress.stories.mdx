import { CircularProgress } from "@chakra-ui/react"
import { Canvas, Meta, Story } from "@storybook/addon-docs"

<Meta title="Atoms/ProgressBars" component={CircularProgress} />

export const Template = (args) => <CircularProgress {...args} />

<Canvas>
  <Story
    name="Circular"
    args={{
      value: 50,
      size: 'sm',
      color: "drab.900",
      trackColor: "drab.700",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
