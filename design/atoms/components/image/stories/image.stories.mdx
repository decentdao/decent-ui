import { Canvas, Meta, Story } from "@storybook/addon-docs"
import { Image } from "@chakra-ui/react"

import tokenPlaceholder from "../../../assets/images/token-default.svg"
import nftPlaceholder from "../../../assets/images/nft-placeholder.svg"

<Meta title="atoms/Images" />

export const Template = (args) => <Image src={args.src} boxSize={args.boxSize} />

### Token Placeholder

Used in place of token logo when unable to retrieve.

<Canvas>
  <Story
    name="Token"
    args={{
      src: tokenPlaceholder,
      boxSize: "1rem",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### NFT Placeholder

This is used in place of a NFT tokenURi that is unavailable. Current viewing sizes are 24px x 24px (1.5rem).
<Canvas>
  <Story
    name="NFT"
    args={{
      src: nftPlaceholder,
      boxSize: "1.5rem"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
