import { Meta, Story, Canvas } from "@storybook/addon-docs"
import { ActiveOne } from "../src/ActiveOne"
import { ATOMS } from "../../../../constants"
import { DESIGN_TOKENS } from "../../../../constants"
import { DisplayIconography } from "./components/DisplayIconography"

<Meta
  title={`Atoms/Icons`}
  component={DisplayIconography}
  parameters={{
    viewMode: "docs",
  }}
/>

## Usage
All Chakra-UI properties will be available, such as `boxSize` and `color`. as well as any spacing options.

```ts
import { ActiveOne } from '@decent-org/fractal-ui'
```

<Canvas>
  <Story name="Usage">
    <ActiveOne boxSize="1.5rem" color="gold.500" />
  </Story>
</Canvas>

## Icons
Use the name shown for the icon to import from `@decent-org/fractal-ui`

<Story name="Icons">
  <DisplayIconography />
</Story>
