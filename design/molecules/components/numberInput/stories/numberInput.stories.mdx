import { Canvas, Meta, Story } from "@storybook/addon-docs"
import { NumberInput, NumberInputField } from "@chakra-ui/react"
import { useState } from "react"
import { argTypes } from "./argTypes"

<Meta title="Molecules/Inputs" component={NumberInput} />

export const Template = (args) => {
  const [value, setValue] = useState("")
  const changeHandler = (value) => {
    setValue(value)
  }
  return (
    <NumberInput size={args.size} disabled={args.disabled} isInvalid={args.isInvalid}>
      <NumberInputField
        placeholder={args.placeholder}
        value={value}
        onChange={(e) => setValue(e.target.value)}
      />
    </NumberInput>
  )
}

# Token Handling Inputs - XL

This XL sized input is for actions like swapping tokens. These would be used in a hero section or anywhere a prominent and robust input is needed.

```ts
import { NumberInput, NumberInputField } from '@chakra-ui/react'
```

<Canvas>
  <Story
    name="NumberInput"
    argTypes={argTypes}
    args={{
      placeholder: "100",
      size: "xl",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
